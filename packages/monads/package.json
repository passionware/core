{
  "name": "@passionware/monads",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "npm run build -- --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "echo 'Add lint script here'",
    "ts": "tsc --noEmit"
  },
  "dependencies": {
    "use-last": "^1.1.1"
  }
}
