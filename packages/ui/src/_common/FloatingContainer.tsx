import { cf } from "@passionware/component-factory";

export const FloatingContainer = cf.div({
  className:
    "overflow-auto z-10 shadow bg-white/90 dark:bg-gray-900/85 backdrop-blur-sm border border-gray-200 dark:border-gray-700 focus:outline-none rounded-lg flex flex-col",
});
